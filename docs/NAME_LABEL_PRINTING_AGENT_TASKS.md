# 学生姓名贴生成与打印：开发 Agent 任务书

> 依赖文档：[架构文档](NAME_LABEL_PRINTING_ARCHITECTURE.md)。所有任务必须遵守项目 `AGENTS.md`；未说明的产品扩展不得自行加入。

## 0. 交付约束

- 目标平台仅为 Web；使用 React 19、TypeScript、Vite、Tailwind v4、React Router、Zustand 与 shadcn/ui 按需组件。
- 功能路由为 `/tools/name-labels`，页面只负责组合，业务代码必须位于 `features/label-printing/`。
- 姓名、原始文件名和导入内容不得写入 URL、控制台、日志、分析事件、`localStorage`、`sessionStorage` 或网络请求。
- 新依赖分两类并保持最小化：NL-00 可按需加入 shadcn/ui 基础组件所需依赖与测试工具链；功能运行时仅加入经过确认的 `react-to-print` 与 SheetJS CE `xlsx`。安装前检查当前依赖，并在变更说明记录用途、版本与许可证。
- 不复制 GPL、未授权或许可证不清晰的参考仓库代码。
- MVP 只实现教师批量姓名贴；单人铺满、桌牌、拼音、班级/学号、卡通主题、背景上传、保存数据和 PDF 输出均不得提前实现，详见架构文档第 4.5 节。

## 1. 进度看板（唯一进度来源）

每次开始、阻塞或完成一个任务时，实施 Agent 必须在本表更新状态；不要只在聊天消息中报告进度。只有该任务的“验收”与“统一完成定义”全部满足后，才能标记为“已完成”。

| 任务 | 状态 | 完成日期 | 验证证据 / 备注 |
| --- | --- | --- | --- |
| NL-00 工程基线、UI 基础与测试工具链 | 未开始 | — | — |
| NL-01 模块骨架、路由与领域类型 | 未开始 | — | — |
| NL-02 纯布局计算、参数校验与测试 | 未开始 | — | — |
| NL-03 有效草稿、表单状态、模板与隐私边界 | 未开始 | — | — |
| NL-04 CSV/XLSX/文本导入与名单清洗 | 未开始 | — | — |
| NL-05 真实尺寸预览与可打印文档 | 未开始 | — | — |
| NL-06 配置面板、页面组合与响应式体验 | 未开始 | — | — |
| NL-07 浏览器打印与校准流程 | 未开始 | — | — |
| NL-08 可访问性、自动化测试与性能检查 | 未开始 | — | — |
| NL-09 文档回填、构建与交付 | 未开始 | — | — |

### 状态填写规则

- `未开始`：尚未改动该任务范围内的代码或配置。
- `进行中`：已开始实施，但验收尚未全部通过；备注写明当前做到哪里。
- `阻塞`：无法继续，备注写明可复现的阻塞原因、已尝试的安全检查和需要的决定/外部条件。
- `已完成`：填写当地日期、关键文件或功能，以及实际执行的测试/构建结果。例如：`2026-08-29｜layout.ts；单元测试、pnpm build、pnpm lint 通过`。

若后续发现已完成任务回归，状态改回 `进行中`，并在备注中注明回归原因。NL-09 完成前，所有前置任务必须为 `已完成`。

## 2. 执行顺序与依赖

```text
NL-00 工程基线、UI 基础与测试工具链
  └── NL-01 模块骨架、路由与领域类型
        └── NL-02 布局计算与校验
              └── NL-03 有效草稿、表单状态与模板
                    ├── NL-04 导入与名单清洗
                    └── NL-05 预览与可打印文档
                          └── NL-06 配置面板与页面组合
                                └── NL-07 打印与校准
                                      └── NL-08 无障碍、测试与验收
```

所有任务完成后执行 NL-09 文档回填与最终验证。

## 3. 任务明细

### NL-00｜工程基线、UI 基础与测试工具链

**进度状态**：以第 1 节进度看板为准。

**目标**：在业务开发前解决基础 UI、测试和版本基线缺失的问题，使后续任务不为测试或对话框临时改变架构。

**修改范围**：根/应用脚本、测试配置、`components/ui/`、全局样式、`app/App.tsx` 的 Provider 挂载，以及必要的 shadcn 配置文件。

**实现要求**：

- 先记录并独立提交当前基础工程基线；功能开发不得与未跟踪的初始化文件混在同一提交中。
- 检查并按需初始化 shadcn/ui，最少提供任务实际需要的 Button、Dialog、Input、Select、Tabs、Tooltip 与 Sonner 挂载；不引入第二套 UI 组件库。
- 建立最小语义设计 token：`surface`、`text`、`border`、`primary`、`error`、`success`、`focus`，并维持 4px/8px 间距节奏。业务组件不得散落色值。
- 配置 Vitest、React Testing Library 与 Playwright，并在根脚本提供 `test`、`test:coverage`、`test:e2e`；测试依赖只服务于当前 Web 应用。
- 恢复 `@typescript-eslint/no-unused-vars` 为错误；`react-hooks/exhaustive-deps` 至少为警告。现有豁免若确有必要，必须在局部代码说明原因。

**验收**：基础工程基线可追溯；一个 Button/Dialog 测试和一个浏览器 smoke test 可运行；`pnpm build`、`pnpm lint` 与新增测试命令均有真实结果。

### NL-01｜模块骨架、路由与领域类型

**进度状态**：以第 1 节进度看板为准。

**目标**：建立可编译但不含业务假实现的 feature 边界与页面入口。

**依赖**：NL-00。

**修改范围**：

- 新建 `apps/web/src/features/label-printing/types.ts`、`index.ts`、对应 `components/`、`hooks/`、`services/`、`stores/`、`utils/` 目录。
- 新建 `apps/web/src/pages/NameLabelPrintingPage.tsx`。
- 在应用路由中注册 `/tools/name-labels`；必要时新增集中式路由常量，不把路径散落在组件里。
- 复用当前全局布局；新增首页/工具目录入口、跳至主内容链接、主内容锚点与未匹配路由回退页，不重写应用壳。

**实现要求**：

- 按架构文档第 6.1 节定义领域类型；不得以 `any` 代替纸张、布局或样式类型。
- 所有尺寸以 `number` + `Mm` 类型语义表示；表单字符串不得进入领域对象。
- 页面仅暂时组合一个 feature 根组件，禁止在 `App.tsx` 放入标签业务代码。

**验收**：访问目标 URL 有清晰页面标题和未导入的空状态；首页可发现该工具；深链和回退页可用；TypeScript 与 Lint 通过。

### NL-02｜纯布局计算、参数校验与测试

**进度状态**：以第 1 节进度看板为准。

**依赖**：NL-01、NL-00。

**目标**：实现屏幕与打印共用的、以毫米为单位的分页算法。

**修改范围**：

- `features/label-printing/utils/layout.ts`
- `features/label-printing/utils/validation.ts`
- 对应单元测试文件（按项目最终测试工具命名）。

**实现要求**：

- 实现 `validateLayout(candidate)`，返回字段级错误而非仅布尔值；校验有限数值、整数行列、正尺寸/间距、页边距、起始格范围与网格容纳关系。
- 实现 `createPageLayouts(names, paper, layout)`，仅接收已验证的领域对象，并依据架构文档第 8.1 节公式生成 `PageLayout[]`；非法候选由验证层以结果对象返回，不抛出未处理异常。
- 以 `firstLabelIndex` 填充首张纸的空格；第二页及之后从第 0 格开始；末页空格明确表示为 `empty`。
- 布局数据必须包含页尺寸、cell 行列号、绝对位置和对应姓名，不让 UI 重新计算位置。
- `offsetXmm`、`offsetYmm` 只在最终位置应用；布局能否容纳不得受偏移影响。
- 无姓名返回空页数组；`PageLayout` 的 cell、页尺寸、位置与姓名引用均为不可变计算结果，UI 不重新计算位置。

**最低测试集**：A4 2×8 单页、跨两页、起始格为中间位置、首格/末格边界、横向纸张、超出可用面积、中文与长姓名、空名单。

**验收**：测试可重复运行，布局结果可由独立 TypeScript 测试验证，不依赖 DOM；测试工具不在本任务临时引入。

### NL-03｜模板预设、Zustand 草稿状态与隐私边界

**进度状态**：以第 1 节进度看板为准。

**依赖**：NL-02。

**目标**：实现“最后有效领域草稿 + 原始表单输入”的双层状态、非敏感模板设置与严格的名单隐私边界。

**修改范围**：

- `features/label-printing/utils/templatePresets.ts`
- `features/label-printing/stores/useLabelPrintingStore.ts`
- `features/label-printing/hooks/useLabelPrintingDraft.ts`

**实现要求**：

- 提供架构文档规定的四种通用 A4 预设与自定义模板；预设数值可编辑。
- 将模板定义为卡片数据：稳定 `id`、纸张与 mm 布局、列×行、每张数量、`maxLines` 内容容量、适用说明和实物验证标记。初版包含 2×8（1–2 行）、3×8（2 行）、4×10（2 行）、3×8（3 行）和自定义；未验证模板不得声称匹配特定厂商纸型。
- Store action 至少包括 `setNames`、`selectTemplate`、`restoreSelectedTemplateDefaults`、`updatePaper`、`updateLayout`、`updateAppearance`、`setPreviewScale`、`resetDraft`。恢复模板默认参数不得清空姓名或起始空白格。
- `useLabelPrintingDraft` 以字符串保存编辑中的字段、触碰状态与字段错误；只有通过 `validateLayout` 的数值才调用 store 更新领域草稿。非法编辑期间，预览使用最后一次有效的 `PageLayout[]`，不能清空姓名或向布局函数传入 `NaN`。
- 不在 store middleware 中加入持久化姓名数据。若实现“记住设置”，只保存纸张、布局、外观与版本号，且写入前显式构造 allowlist 对象。
- `resetDraft` 必须恢复安全默认模板并删除内存中的姓名。

**验收**：模板卡显示列×行、每张数量、内容行数与验证状态；恢复当前模板默认参数后，姓名和起始空白格不变；输入非法数值时保留最后有效预览且显示字段错误。用浏览器开发工具确认修改姓名后没有新增存储键、没有网络请求、刷新后姓名消失；非敏感设置若实现持久化，刷新后可恢复。

### NL-04｜CSV/XLSX/文本导入与名单清洗

**进度状态**：以第 1 节进度看板为准。

**依赖**：NL-01、NL-03。

**目标**：把所有输入渠道归一为带源行号的 `StudentName[]`。

**修改范围**：

- `features/label-printing/services/importNames.ts`
- `features/label-printing/hooks/useLabelImport.ts`
- `LabelImportPanel.tsx`、`NameColumnPicker.tsx`、`NameListPreview.tsx`
- `workers/parseWorkbook.worker.ts`（仅在主线程测量显示明显卡顿时创建）、必需的新依赖与锁文件。

**实施前检查**：先检查 `apps/web/package.json` 是否已有可用解析能力。若没有，使用 Apache-2.0 的 SheetJS CE `xlsx`；只在文件导入时动态加载，并在变更说明记录版本、许可证和最终产物体积。不得为了 CSV 另加第二个解析库。

**实现要求**：

- 支持多行粘贴、CSV、XLSX 第一个工作表；CSV 支持常规带引号字段，不能用简单 `split(',')` 代替解析器。
- 只接受 CSV/XLSX；导入前拒绝大于 5 MB 的文件，解析最多 10,000 行、100 列。超过限制、损坏文件或不支持格式时，错误不得含姓名/原始表格内容，且不改变当前草稿。
- 表格导入显示列名及最多 5 个非敏感示例值，用户显式选择姓名列后才写入草稿。
- 清洗：去首尾空白/不可见空格、NFC 规范化、移除空行、限制最大 80 字符、保留原始行号。
- 同名记录默认保留并标记重复，不能静默去重。
- 解析失败或列选择取消时保留原草稿；错误说明原因和可操作的恢复方式。
- 文件读取只用 File API，不上传文件；错误对象不可包含姓名或原始表格内容。
- 对 1,000 行和接近上限的 XLSX 测量解析与输入响应；若主线程出现明显卡顿，将解析移入 feature 私有 Worker，Worker 只处理当前内存中的受限数据。

**验收**：使用中文姓名、英文姓名、空行、重复值、带引号的 CSV、两列 XLSX、超限文件与损坏文件进行手工检查；导入后没有网络请求与持久化姓名。

### NL-05｜真实尺寸预览与可打印文档

**进度状态**：以第 1 节进度看板为准。

**依赖**：NL-02、NL-03。

**目标**：由同一 `PageLayout[]` 渲染屏幕预览和打印内容。

**修改范围**：

- `PrintPreview.tsx`
- `PrintableLabelDocument.tsx`
- `styles/print.css`

**实现要求**：

- 屏幕预览可缩放，缩放通过外层 CSS transform 实现，不修改 mm 领域数据。
- 可打印文档以 `paper.widthMm`、`paper.heightMm` 建立每一页，cell 使用稳定 id 与 mm 定位。
- 姓名一律作为文本节点输出；允许换行由 `allowWrap` 决定。长姓名不能撑破标签；展示溢出策略须可预测。
- 模板的 `maxLines` 必须约束推荐字号、行高、内边距与内容溢出策略；`3×8｜3 行内容` 与 `3×8｜2 行内容` 是不同预设，不能通过运行时任意缩小字体混用。
- `@media print` 隐藏导航、配置面板、缩放控件、辅助线和屏幕专用提示；只打印姓名贴文档。
- `@page`、纸张方向、打印 document 标题和背景图形提示符合架构文档第 8.3 节。
- 若布局无效或名单为空，不渲染可打印内容，不允许打印按钮可用。

**验收**：预览页数/起始空白格与 NL-02 单元测试一致；浏览器打印预览不出现应用 Header、按钮或输入框。

### NL-06｜配置面板、页面组合与响应式体验

**进度状态**：以第 1 节进度看板为准。

**依赖**：NL-03、NL-04、NL-05。

**目标**：形成完整教师工作台，不把所有控件堆在一个无层级表单中。

**修改范围**：

- `NameLabelPrintingPage.tsx`
- `LabelTemplatePanel.tsx`、`LabelContentPanel.tsx` 与必要的 feature 子组件。
- 仅在缺少时，按项目规范添加 shadcn/ui 组件源码。

**实现要求**：

- 桌面 `>=1024px` 为“左配置、右预览”，右侧打印主操作始终明显；中小屏纵向排列。
- 空状态加入可跳转、非锁定的四步工作流提示：录入名单、选择标签纸、调整样式、打印 A4。它只能帮助理解流程，不能阻止教师回退修改前面的面板。
- 标签纸面板首先显示模板卡片库；卡片包含缩略网格、列×行、每张数量、内容行数、适用说明和实物验证状态。模板超过一屏时按“全部 / 1 行 / 2 行 / 3 行 / 4 行内容”筛选；不显示伪精确的使用次数或热度。
- 配置按“名单与导入 / 标签纸与排版 / 内容与样式 / 校准”分组，使用渐进披露，初次进入不展开不必要的高级参数；参数区提供“恢复当前模板默认参数”，并说明该操作保留姓名名单和起始格。
- 每个可编辑数值字段有名称、单位、允许范围与内联错误；表单错误不只用 toast。
- 严格实现架构文档第 4.4 节：浅色工作区、白色纸张预览、4px/8px 间距节奏和唯一的“打印姓名贴”主操作；不擅自引入深色主题、外部字体或装饰性渐变。
- 使用 NL-00 的语义 token，不在 feature 组件中散落色值；缩放、筛选和操作控件的键盘焦点不能被黏性预览或顶部导航遮挡，并在减少动态效果设置下关闭非必要过渡。
- 打印校验失败时，在配置区顶部呈现可聚焦的 `role="alert"` 错误摘要，并链接至每个字段；焦点移至摘要，字段内联错误继续保留。
- 清空名单显示确认；完成后提供短时撤销；Toast 不抢夺键盘焦点。
- 使用现有 `lucide-react` 图标，不引入第二套图标库或 emoji 结构图标。
- 标准控件与图标按钮都提供 hover、focus、disabled 状态，保持明确对比度。

**验收**：在 375px、768px、1024px、1440px 检查布局；页面主体无无意横向滚动，触控主要目标至少 44px。

### NL-07｜浏览器打印与校准流程

**进度状态**：以第 1 节进度看板为准。

**依赖**：NL-05、NL-06。

**目标**：接入安全、可恢复的打印调用与校准页。

**修改范围**：

- `features/label-printing/services/labelPrintService.ts`
- `features/label-printing/hooks/useNameLabelPrint.ts`
- `PrintCalibrationPanel.tsx`、`PrintableCalibrationDocument.tsx`
- 依赖与锁文件。

**实施前检查**：确认 `react-to-print` v3 的 React 19 兼容版本与 MIT 许可证，并锁定实际安装版本；优先只新增该打印依赖。不要在本任务加入 jsPDF。

**实现要求**：

- 将专用可打印文档 ref 交给打印 hook，设置动态 `pageStyle` 和有意义的文档标题。
- 在 Chromium 与 Edge 中验证打印 iframe 能收到所需的 `@page`、字体、mm 尺寸和打印专用样式；打印样式不得依赖未复制到 iframe 的父级选择器。
- 打印进行中禁用重复点击；失败时保留当前草稿、显示重试操作。
- 校准页不含姓名，使用当前纸张/布局边界、基准线和 X/Y 偏移说明。
- 偏移范围为 `-10mm` 到 `10mm`、步长 `0.1mm`，支持正负输入，并在预览中反映。
- 打印前显示一次简短提示：纸张缩放使用 100%、关闭“适合页面”；背景样式需要时开启背景图形。
- 将“进纸方式”和“是否支持自定义纸张”转换为打印兼容说明：用户在打印机驱动/系统对话框确认，页面不得声称能检测或控制硬件能力。为纸张、页边距、标签间距、起始格和校准偏移提供可见文字帮助。
- 提示只面向本次浏览器打印，不得借此实现“导出 PDF”或保存名单等 MVP 之外的动作。

**验收**：在浏览器打印预览检查 A4 纵/横向、多个页面、起始格与校准页；模拟/实际取消打印不会清空名单或破坏 UI 状态。

### NL-08｜可访问性、自动化测试与性能检查

**进度状态**：以第 1 节进度看板为准。

**依赖**：NL-04 至 NL-07。

**目标**：验证交互、隐私和大名单场景，而不只验证视觉呈现。

**修改范围**：单元/组件/E2E 测试、必要的无障碍修复与性能测量；测试配置只在 NL-00 已确认的工具确有缺口时最小化调整。

**实现要求**：

- 使用 NL-00 已配置的 Vitest、React Testing Library 与 Playwright；禁止在本任务临时替换测试框架或进行无关依赖升级。
- 覆盖 NL-02 与 NL-04 的单元场景，以及导入到打印按钮可用的核心组件流程；模拟打印回调而不是尝试自动化系统打印对话框。
- 覆盖模板卡元数据、2×8/3×8/4×10 分页、不同内容行数的溢出处理，以及恢复当前模板默认参数仍保留名单/起始格的场景。
- 增加至少一个测试/检查：姓名内容不出现于 URL 或持久化存储。
- 键盘走查：导入、列选择、字段错误、清空确认、打印和关闭对话框；焦点不能被黏性元素遮挡。
- 对 1,000 个姓名测量/观察预览响应；必要时仅虚拟化屏幕预览，打印文档不可漏页。对超限文件确认在解析前拒绝。

**验收**：`test`、`test:coverage`、`test:e2e` 通过；至少在 Chrome/Edge 各检查一次打印预览，并在一台常用 A4 打印机记录纸张、缩放、驱动与校准偏差；无控制台错误或泄露的敏感内容。

### NL-09｜文档回填、构建与交付

**进度状态**：以第 1 节进度看板为准。

**依赖**：NL-00 至 NL-08。

**目标**：确保代码与文档一致，并输出可复现的验证结果。

**修改范围**：

- 根据实际实现更新 `NAME_LABEL_PRINTING_ARCHITECTURE.md` 中已变化的事实。
- 更新 `README.md` 或用户可发现的工具入口（仅当首页/导航已实际加入入口）。
- 如新增依赖、路线或重要决定，更新相应文档。

**必须执行**：

```bash
corepack pnpm build
corepack pnpm lint
```

同时执行新测试命令，并手工检查实际打印预览。任何不能运行的检查必须写明原因，不能伪称通过。

**交付说明必须包含**：改动的文件、支持的导入格式与资源上限、实际安装依赖/许可证、已实测的浏览器/打印机/纸张/缩放条件、未验证的纸张模板、隐私边界、验证命令结果与后续待决项。

## 4. 统一完成定义

任一任务仅在以下条件都满足时可标记完成：

- 改动被限制在任务范围内，无无关重构。
- TypeScript 类型明确，没有新增 `any` 或忽略式类型断言。
- UI 使用现有主题与 shadcn/ui 策略，无第二套 UI 框架。
- 学生姓名不离开浏览器内存边界。
- 非法表单输入没有进入布局算法，预览在编辑期间保持最后有效布局。
- 导入文件、行列数和文本长度均受到既定上限约束；失败或超限不泄露原始内容。
- 相关自动化测试、构建和 Lint 已运行并报告真实结果。
- 文档中的架构、任务状态与实际实现没有冲突。

## 5. 禁止事项

- 不把 CSV/XLSX 解析、打印调用、Supabase 查询写入页面或通用 UI 组件。
- 不引入数据库/Storage 以“方便保存导入文件”为理由。
- 不用像素替代纸张和标签的业务尺寸。
- 不依赖浏览器缩放自动修复打印偏差，也不承诺未经实测的厂商标签模板精度。
- 不把 PDF 生成功能、二维码、班级管理、云端共享、单人铺满、桌牌、拼音、班级/学号字段、主题模板或背景上传混入 MVP。
